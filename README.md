# 轻松农场作物成熟追踪（浏览器扩展）

记录“黑与白福利站”轻松农场全部地块的作物成熟时间，在扩展图标上显示最近成熟作物的倒计时，点击弹出紧凑面板查看地块成熟时间与提醒。

## 特性

- **被动记录**：通过 hook 页面自身的 `fetch`/`XMLHttpRequest` 捕获 `/api/farm/crops` 响应（含 `/api/batch` 聚合响应），不主动调用后端接口、不执行任何页面操作。
- **页面数据优先**：播种/收获可能在其他客户端进行；以页面数据为准，本地记录冲突时覆盖。
- **去重压缩**：播种是批量进行的，相同成熟时间（按作物/等级/成熟时刻）聚合数量。
- **图标徽章**：显示最近一个成熟作物的倒计时，成熟后变红并显示“熟”，10 分钟内成熟为橙色，其余为绿色。
- **作物图片**：优先使用页面数据指向的站点静态成熟图标，新作物无需随扩展补图。站点 PNG 首次成功加载后会以 data URL 永久缓存在扩展本地，后续面板直接使用缓存；未缓存的图标才重新加载。加载失败时回退到随包作物图标，再回退到通用图标；只允许站点 `/farm/crops/` 下的 PNG，不查询图片接口。已有记录缺少图片字段时按作物 ID 推导地址。
- **紧凑面板**：聚合后的地块列表直接平铺展示，逐条显示等级、数量、剩余时间（彩色）与成熟时刻（绝对时间）。
- **提醒**：支持 0/多 个提醒，可设「提前/滞后」相对最近成熟时间的秒数，逐条启用/禁用、预览预计提醒时间、上移/下移/删除（悬停显示操作），触发浏览器通知，点击通知跳转农场标签页（或新开）。

## 安装（开发）

Chrome（Manifest V3）：

1. 打开 `chrome://extensions`
2. 右上角开启「开发者模式」
3. 「加载已解压的扩展程序」→ 选择本目录 `src/`

> 后台为 MV3 service worker，会被休眠；徽章/提醒由 `chrome.alarms`（最小 1 分钟）驱动刷新，因此图标徽章倒计时以分钟粒度更新。

## 目录

- `manifest.json`（MV3）、`browser-shim.js`（Firefox/Chromium 兼容层）、`background.js`（service worker）、`content.js`、`popup.*`、`icons/`

详见各文件头注释与 `src/AGENTS.md`。
